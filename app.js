const swiper = new Swiper('.swiper', {
    loop: true,
    pagination: {
      el: '.swiper-pagination',
    },
    grabCursor: true,
    autoplay: true,
    effect: 'cube', 
    speed: 1000,
  })

  // fade, flip, cube, coverflow, cards